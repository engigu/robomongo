# Robo 3T v1.5.4 发行说明

## 版本摘要
*   **版本号**：1.5.4
*   **发布日期**：2026-04-20
*   **主要特性**：首选项功能入口恢复与设置路径优化 (Preferences Entry Restored)

## 本次更新亮点 (1.5.4)

### 1. 恢复“首选项 (Preferences)”菜单入口
*   **入口开放**：修正了主窗口“选项 (Options)”菜单中“首选项”项被错误隐藏的问题。现在用户可以正常通过 GUI 进入设置面板。
*   **功能整合**：通过“首选项”面板，您可以一站式管理字体族、字号大小、默认视图模式等全局配置。

### 2. 字体自定义功能优化
*   **持久化修复**：解决了重启后字体设置（系列和字号）丢失的问题。
*   **交互优化**：优化了设置迁移逻辑，确保切换即时生效且配置稳定保存。

### 3. 编辑布局恢复 (UX Improvement)
*   **拖拽功能恢复**：重新引入了垂直分割器 (QSplitter)，恢复了查询编辑器与结果面板之间的拖拽缩放功能。
*   **自适应高度优化**：移除了编辑器的强制固定高度限制，将布局控制权交还给用户，使专业用户能够更灵活地分配屏幕空间。

### 4. 查询结果标签命名优化 (Tab Naming Optimization)
*   **智能命名**：现在查询结果标签会优先显示所查询的集合名称（表名），例如 `Device` 或 `ConsumeRecord`，而不再是通用的 `db.getColl..`。
*   **悬浮提示保持**：虽然标签标题变得精简，但鼠标悬浮在标签上时，依然可以查看原始的完整 JavaScript 语句，兼顾了易读性与完整信息。
*   **显示平衡**：集合名称显示长度优化为 16 个字符，既能提供足够的信息，又不会过度占用标签空间。
